query Input {
  cart {
    lines {
      id
      quantity
      cost {
        subtotalAmount {
          amount
        }
      }
      attribute(key: "VAT Relief") {
        value
      }
    }
  }
  discount {
    discountClasses
  }
}
